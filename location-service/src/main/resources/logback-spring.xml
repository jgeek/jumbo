<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Define properties for different environments -->
    <springProfile name="local,dev">
        <property name="LOG_LEVEL_ROOT" value="INFO"/>
        <property name="LOG_LEVEL_JUMBO" value="DEBUG"/>
        <property name="LOG_LEVEL_SPRING" value="INFO"/>
        <property name="LOG_LEVEL_WEB" value="DEBUG"/>
        <property name="LOG_LEVEL_SQL" value="DEBUG"/>
        <property name="LOG_FILE" value="${user.dir}/logs/jumbo-location-service"/>
        <property name="MAX_FILE_SIZE" value="10MB"/>
        <property name="MAX_HISTORY" value="10"/>
    </springProfile>

    <springProfile name="test">
        <property name="LOG_LEVEL_ROOT" value="WARN"/>
        <property name="LOG_LEVEL_JUMBO" value="INFO"/>
        <property name="LOG_LEVEL_SPRING" value="WARN"/>
        <property name="LOG_LEVEL_WEB" value="WARN"/>
        <property name="LOG_LEVEL_SQL" value="WARN"/>
        <property name="LOG_FILE" value="${user.dir}/logs/jumbo-location-service-test"/>
        <property name="MAX_FILE_SIZE" value="5MB"/>
        <property name="MAX_HISTORY" value="5"/>
    </springProfile>

    <springProfile name="docker,staging">
        <property name="LOG_LEVEL_ROOT" value="INFO"/>
        <property name="LOG_LEVEL_JUMBO" value="INFO"/>
        <property name="LOG_LEVEL_SPRING" value="WARN"/>
        <property name="LOG_LEVEL_WEB" value="INFO"/>
        <property name="LOG_LEVEL_SQL" value="WARN"/>
        <property name="LOG_FILE" value="${user.dir}/logs/jumbo-location-service"/>
        <property name="MAX_FILE_SIZE" value="50MB"/>
        <property name="MAX_HISTORY" value="30"/>
    </springProfile>

    <springProfile name="docker,prod,production">
        <property name="LOG_LEVEL_ROOT" value="WARN"/>
        <property name="LOG_LEVEL_JUMBO" value="INFO"/>
        <property name="LOG_LEVEL_SPRING" value="ERROR"/>
        <property name="LOG_LEVEL_WEB" value="WARN"/>
        <property name="LOG_LEVEL_SQL" value="ERROR"/>
        <property name="LOG_FILE" value="${user.dir}/logs/jumbo-location-service"/>
        <property name="MAX_FILE_SIZE" value="100MB"/>
        <property name="MAX_HISTORY" value="60"/>
    </springProfile>

    <!-- Console appender for development -->
    <springProfile name="local,dev,test">
        <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <encoder class="net.logstash.logback.encoder.LoggingEventCompositeJsonEncoder">
                <providers>
                    <timestamp/>
                    <logLevel/>
                    <loggerName/>
                    <message/>
                    <mdc/>
                    <arguments/>
                    <stackTrace/>
                </providers>
            </encoder>
        </appender>
    </springProfile>

    <!-- JSON Console appender for production environments -->
    <springProfile name="docker,staging,prod,production">
        <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <encoder class="net.logstash.logback.encoder.LoggingEventCompositeJsonEncoder">
                <providers>
                    <timestamp/>
                    <logLevel/>
                    <loggerName/>
                    <message/>
                    <mdc/>
                    <arguments/>
                    <stackTrace/>
                </providers>
            </encoder>
        </appender>
    </springProfile>

    <!-- File appender with rolling policy -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_FILE}.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_FILE}.%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>
            <maxFileSize>${MAX_FILE_SIZE}</maxFileSize>
            <maxHistory>${MAX_HISTORY}</maxHistory>
            <totalSizeCap>1GB</totalSizeCap>
        </rollingPolicy>
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %X{traceId:-} %X{spanId:-} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Error file appender for errors and above -->
    <appender name="ERROR_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_FILE}-error.log</file>
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>ERROR</level>
        </filter>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_FILE}-error.%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>
            <maxFileSize>${MAX_FILE_SIZE}</maxFileSize>
            <maxHistory>${MAX_HISTORY}</maxHistory>
            <totalSizeCap>500MB</totalSizeCap>
        </rollingPolicy>
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %X{traceId:-} %X{spanId:-} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Async appender for better performance -->
    <appender name="ASYNC_FILE" class="ch.qos.logback.classic.AsyncAppender">
        <appender-ref ref="FILE"/>
        <queueSize>512</queueSize>
        <discardingThreshold>0</discardingThreshold>
        <includeCallerData>true</includeCallerData>
    </appender>

    <appender name="ASYNC_ERROR_FILE" class="ch.qos.logback.classic.AsyncAppender">
        <appender-ref ref="ERROR_FILE"/>
        <queueSize>256</queueSize>
        <discardingThreshold>0</discardingThreshold>
        <includeCallerData>true</includeCallerData>
    </appender>

    <!-- Loggers for your application packages -->
    <logger name="com.jumbo" level="${LOG_LEVEL_JUMBO}" additivity="false">
        <springProfile name="local,dev,test">
            <appender-ref ref="CONSOLE"/>
        </springProfile>
        <appender-ref ref="ASYNC_FILE"/>
        <appender-ref ref="ASYNC_ERROR_FILE"/>
    </logger>

    <!-- Spring Framework loggers -->
    <logger name="org.springframework" level="${LOG_LEVEL_SPRING}" additivity="false">
        <springProfile name="local,dev">
            <appender-ref ref="CONSOLE"/>
        </springProfile>
        <appender-ref ref="ASYNC_FILE"/>
    </logger>

    <logger name="org.springframework.web" level="${LOG_LEVEL_WEB}" additivity="false">
        <springProfile name="local,dev">
            <appender-ref ref="CONSOLE"/>
        </springProfile>
        <appender-ref ref="ASYNC_FILE"/>
    </logger>

    <logger name="org.springframework.security" level="WARN" additivity="false">
        <springProfile name="local,dev">
            <appender-ref ref="CONSOLE"/>
        </springProfile>
        <appender-ref ref="ASYNC_FILE"/>
    </logger>

    <logger name="org.springframework.boot" level="INFO" additivity="false">
        <springProfile name="local,dev">
            <appender-ref ref="CONSOLE"/>
        </springProfile>
        <appender-ref ref="ASYNC_FILE"/>
    </logger>

    <logger name="org.springframework.boot.autoconfigure" level="WARN" additivity="false">
        <springProfile name="local,dev">
            <appender-ref ref="CONSOLE"/>
        </springProfile>
        <appender-ref ref="ASYNC_FILE"/>
    </logger>

    <!-- Database and SQL loggers -->
    <logger name="org.hibernate" level="${LOG_LEVEL_SQL}" additivity="false">
        <springProfile name="local,dev">
            <appender-ref ref="CONSOLE"/>
        </springProfile>
        <appender-ref ref="ASYNC_FILE"/>
    </logger>

    <logger name="org.hibernate.SQL" level="${LOG_LEVEL_SQL}" additivity="false">
        <springProfile name="local,dev">
            <appender-ref ref="CONSOLE"/>
        </springProfile>
        <appender-ref ref="ASYNC_FILE"/>
    </logger>

    <logger name="org.hibernate.type.descriptor.sql.BasicBinder" level="${LOG_LEVEL_SQL}" additivity="false">
        <springProfile name="local,dev">
            <appender-ref ref="CONSOLE"/>
        </springProfile>
        <appender-ref ref="ASYNC_FILE"/>
    </logger>

    <!-- HTTP Client loggers -->
    <logger name="org.apache.http" level="WARN" additivity="false">
        <springProfile name="local,dev">
            <appender-ref ref="CONSOLE"/>
        </springProfile>
        <appender-ref ref="ASYNC_FILE"/>
    </logger>

    <logger name="org.apache.http.wire" level="WARN" additivity="false">
        <springProfile name="local,dev">
            <appender-ref ref="CONSOLE"/>
        </springProfile>
        <appender-ref ref="ASYNC_FILE"/>
    </logger>

    <!-- Tomcat/Undertow loggers -->
    <logger name="org.apache.tomcat" level="WARN" additivity="false">
        <springProfile name="local,dev">
            <appender-ref ref="CONSOLE"/>
        </springProfile>
        <appender-ref ref="ASYNC_FILE"/>
    </logger>

    <logger name="io.undertow" level="WARN" additivity="false">
        <springProfile name="local,dev">
            <appender-ref ref="CONSOLE"/>
        </springProfile>
        <appender-ref ref="ASYNC_FILE"/>
    </logger>

    <!-- Netty loggers (if using WebFlux) -->
    <logger name="io.netty" level="WARN" additivity="false">
        <springProfile name="local,dev">
            <appender-ref ref="CONSOLE"/>
        </springProfile>
        <appender-ref ref="ASYNC_FILE"/>
    </logger>

    <!-- JSON processing loggers -->
    <logger name="com.fasterxml.jackson" level="WARN" additivity="false">
        <springProfile name="local,dev">
            <appender-ref ref="CONSOLE"/>
        </springProfile>
        <appender-ref ref="ASYNC_FILE"/>
    </logger>

    <!-- Swagger/OpenAPI loggers -->
    <logger name="io.swagger" level="WARN" additivity="false">
        <springProfile name="local,dev">
            <appender-ref ref="CONSOLE"/>
        </springProfile>
        <appender-ref ref="ASYNC_FILE"/>
    </logger>

    <logger name="springfox" level="WARN" additivity="false">
        <springProfile name="local,dev">
            <appender-ref ref="CONSOLE"/>
        </springProfile>
        <appender-ref ref="ASYNC_FILE"/>
    </logger>

    <!-- Micrometer/Actuator loggers -->
    <logger name="io.micrometer" level="WARN" additivity="false">
        <springProfile name="local,dev">
            <appender-ref ref="CONSOLE"/>
        </springProfile>
        <appender-ref ref="ASYNC_FILE"/>
    </logger>

    <!-- Root logger -->
    <root level="${LOG_LEVEL_ROOT}">
        <springProfile name="local,dev,test">
            <appender-ref ref="CONSOLE"/>
        </springProfile>
        <springProfile name="docker,staging,prod,production">
            <appender-ref ref="CONSOLE"/>
        </springProfile>
        <appender-ref ref="ASYNC_FILE"/>
        <appender-ref ref="ASYNC_ERROR_FILE"/>
    </root>

</configuration>
